### FYI
This simple node app need to use "Express" framework as minimum to run app normally

--------------------------------------- And ---------------------------------------

Use "Babel" to make build app to prevent source code leaking to public
